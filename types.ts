export interface Option {
  id: string;
  label: string;
  imageUrl: string;
  prompt: string;
}

export interface ProductDetails {
  productType: string;
  color: string;
  style: string;
  modelPose: string;
  lightingStyle: string;
  shotType: string;
}